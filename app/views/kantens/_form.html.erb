<%= form_for(@kanten) do |f| %>
  <% if @kanten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kanten.errors.count, "error") %> prohibited this kanten from being saved:</h2>

      <ul>
      <% @kanten.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br />
    <%= f.text_field :start %>
  </div>
  <div class="field">
    <%= f.label :ziel %><br />
    <%= f.text_field :ziel %>
  </div>
  <div class="field">
    <%= f.label :laenge %><br />
    <%= f.text_field :laenge %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
